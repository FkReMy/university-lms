{
  // JS project config for editor IntelliSense / path aliases.
  // Notes:
  // - TypeScript 6 deprecates baseUrl/moduleResolution=node10. We keep baseUrl for paths
  //   and silence the notice with ignoreDeprecations: "6.0".
  // - moduleResolution "bundler" matches Vite/ESM setups.
  "compilerOptions": {
    "ignoreDeprecations": "6.0",

    // Path alias base (keep "." so aliases resolve from project root)
    "baseUrl": ".",

    // Aliases (map "@/..." to src/...)
    "paths": {
      "@/*": ["src/*"],
      "@/assets/*": ["src/assets/*"],
      "@/styles/*": ["src/styles/*"],
      "@/lib/*": ["src/lib/*"],
      "@/services/*": ["src/services/*"],
      "@/store/*": ["src/store/*"],
      "@/hooks/*": ["src/hooks/*"],
      "@/router/*": ["src/router/*"],
      "@/components/*": ["src/components/*"],
      "@/pages/*": ["src/pages/*"]
    },

    // JS/JSX settings
    "jsx": "react-jsx",
    "module": "esnext",
    "moduleResolution": "bundler",
    "target": "esnext",

    // Useful flags
    "allowSyntheticDefaultImports": true,
    "resolveJsonModule": true,
    "checkJs": false,

    // Prevent TS from trying to auto-include missing @types (avoids the jsx-a11y type lookup)
    "types": []
  },

  // Scope JS/JSX files
  "include": ["src"],

  // Exclude build and dependencies
  "exclude": ["node_modules", "dist"],

  // Optional: further restrict automatic type acquisition
  "typeAcquisition": {
    "enable": true,
    "exclude": ["eslint-plugin-jsx-a11y"]
  }
}