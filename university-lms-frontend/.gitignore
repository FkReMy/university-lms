# .gitignore for University LMS Frontend (Production-Ready)
# ------------------------------------------------------------------
# Ignores all files/folders that should NOT be tracked in Git for a
# React + Vite + Node.js project. 
# - Keeps repo clean and secure.
# - Keeps example env files, but never secrets.
# - All system/tool/editor artifacts excluded.

# Node & package manager dependencies
node_modules/
package-lock.json
yarn.lock
pnpm-lock.yaml
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build & Vite outputs (never tracked in Git)
dist/
dist-ssr/
*.local

# Environment files (Keep only safe example in VCS)
.env
.env.*
!.env.example

# Editor/project files and OS junk
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# Log files and log folders
logs/
*.log
*.log.*

# Optional: coverage, test outputs (uncomment if needed)
# coverage/
# jest-*.json